# Other utilities ###########################################
# man command open vim
export MANPAGER="/bin/sh -c \"col -b -x|vim -R -c 'set ft=man nolist nonu noma' -\""

# my command
export PATH=$PATH:${HOME}/tools

